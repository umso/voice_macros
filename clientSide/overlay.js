function showClickOverlay(element) {
	console.log(element);
}

function showClickWhenOverlay(selection) {
	console.log(selection);
}

function showSShotOverlay(selection) {
	console.log(selection);
}

function showTTSOverlay(selection) {
	console.log(selection);
}
